[package]
name = "poc"
version = "0.1.0"
style_edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
inner = { path = "../inner" }
katex-support = { path = "katex-support" }
nested_mod = { path = "nested_mod" }
