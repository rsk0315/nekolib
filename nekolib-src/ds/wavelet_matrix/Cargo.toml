[package]
name = "wavelet_matrix"
version = "0.1.0"
style_edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rs01_dict = { path = "../rs01_dict" }
usize_bounds = { path = "../../ops/usize_bounds" }
