[package]
name = "foldable_deque"
version = "0.1.0"
style_edition = "2024"

[dependencies]
monoid = { path = "../../ops/monoid" }

[dev-dependencies]
concat-monoid = { path = "../../naive/concat-monoid" }
