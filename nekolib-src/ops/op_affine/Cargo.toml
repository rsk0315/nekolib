[package]
name = "op_affine"
version = "0.1.0"
style_edition = "2024"
edition = "2024"

[dependencies]
monoid = { path = "../monoid" }
has_zero = { path = "../has_zero" }
has_one = { path = "../has_one" }
